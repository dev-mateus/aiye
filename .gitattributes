# Configuração para Xet storage (Hugging Face)
# Xet é recomendado para arquivos grandes ao invés de Git LFS

# PDFs - usar Xet storage
backend/data/pdfs/*.pdf filter=xet diff=xet merge=xet -text
# FAISS index files (se commitados no futuro)
backend/data/index/*.faiss filter=xet diff=xet merge=xet -text
# Arquivos grandes de modelos (se houver)
*.bin filter=xet diff=xet merge=xet -text
*.pt filter=xet diff=xet merge=xet -text
*.pth filter=xet diff=xet merge=xet -text
backend/data/pdfs/*.pdf filter=lfs diff=lfs merge=lfs -text
backend/data/index/*.faiss filter=lfs diff=lfs merge=lfs -text
