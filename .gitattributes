# Configuração para Git LFS (Hugging Face Spaces)
# Arquivos grandes devem usar LFS para evitar rejeição no push

# PDFs - usar LFS
backend/data/pdfs/*.pdf filter=lfs diff=lfs merge=lfs -text

# FAISS index files
backend/data/index/*.faiss filter=lfs diff=lfs merge=lfs -text
backend/data/index/*.json filter=lfs diff=lfs merge=lfs -text

# Arquivos grandes de modelos
*.bin filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
